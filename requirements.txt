# ── ONNX Runtime EP 선택 (플랫폼별 하나만 설치) ──────────────────────────────
# RPi5 / ARM64 CPU:        pip install onnxruntime>=1.20.1
# Linux x86 CUDA:          pip install onnxruntime-gpu==1.18.0
# Windows DX12 (no CUDA):  pip install onnxruntime-directml>=1.20.1
# Docker ai-qwen 기본값:   Dockerfile에서 onnxruntime-gpu → onnxruntime 치환 (ARM64 빌드)
onnxruntime-gpu==1.18.0
# ─────────────────────────────────────────────────────────────────────────────
transformers>=4.46.0,<4.48.0
tokenizers>=0.20.0,<0.22.0
numpy==1.26.4
redis==5.0.3
huggingface-hub>=0.24.0,<1.0
regex>=2022.1.18
optimum>=1.17.0,<1.18.0
psutil>=5.9.0
# Task C (GGUF path): uncomment after Task B latency results confirm GGUF advantage
# llama-cpp-python>=0.3.0

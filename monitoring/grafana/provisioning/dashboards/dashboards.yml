apiVersion: 1

providers:
  - name: qwen-llmops
    orgId: 1
    folder: LLMOps
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false      # UI 수정 금지 — 대시보드는 코드가 원본
    options:
      path: /etc/grafana/dashboards
      foldersFromFilesStructure: false
